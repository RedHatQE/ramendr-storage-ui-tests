diff --git a/values-hub.yaml b/values-hub.yaml
index 55e8760..3acf1ce 100644
--- a/values-hub.yaml
+++ b/values-hub.yaml
@@ -21,10 +21,12 @@ clusterGroup:
     odf-multicluster-orchestrator:
       name: odf-multicluster-orchestrator
       namespace: openshift-operators
+      channel: stable-4.20
 
     openshift-data-foundation:
       name: odf-operator
       namespace: openshift-storage
+      channel: stable-4.20
 
   argoCD:
     resourceTrackingMethod: annotation
@@ -129,6 +131,7 @@ clusterGroup:
       extraValueFiles:
         - '/overrides/values-cluster-names.yaml'
         - '/overrides/values-egv-dr.yaml'
+        - '/overrides/values-aws-cost-optimized.yaml'
       ignoreDifferences:
         # Prevent ArgoCD from pruning dynamically created OCM resources
         # These resources are created by OCM operators and should never be pruned
